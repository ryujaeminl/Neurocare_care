# Android install file

`neurocare-guardian-v2.20.apk` is the guardian app (`com.neurocare.guardian`).

It is debug-signed so it can be installed directly for testing. Android may require enabling installation from the source used to download the file.
